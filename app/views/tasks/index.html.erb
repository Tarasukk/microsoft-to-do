<div class="d-flex">
  <div class="w-25">
    <%= render "tasks/menu"%>
  </div>
  <div class="w-75  task-menu-own-gray vh-100 p-4">
    <h3 class="mb-4 mt-2"><i class="fa-solid fa-house m-1" style="color:#2564cf;"></i> Завдання</h3>
    <%= render "tasks/form" %>
    <div id="tasks" class="d-flex flex-column gap-3 ">
      <% @tasks.each do |task| %>
        <%= render task %>
      <% end %>
    </div>
    <%# <%= link_to "New task", new_task_path %>
  </div>
</div>
